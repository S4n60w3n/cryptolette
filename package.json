{
  "name": "truffle-quickstart",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "truffle develop",
    "compile": "truffle compile",
    "test": "npm run compile && jest --forceExit",
    "test:watch": "jest --watch",
    "test:all": "truffle test && cd client && npm run test",
    "migrate": "truffle migrate",
    "migrate:ganache": "truffle migrate ganache"
  },
  "dependencies": {
    "@chainlink/contracts": "0.1.9",
    "@openzeppelin/test-helpers": "0.5.11"
  },
  "devDependencies": {
    "@openzeppelin/test-environment": "0.1.9",
    "@types/jest": "26.0.23",
    "jest": "27.0.5",
    "truffle": "5.3.11",
    "ts-jest": "27.0.3",
    "typescript": "4.3.4"
  }
}
